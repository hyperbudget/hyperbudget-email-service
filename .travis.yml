language: python
python:
- "3.6"
services:
  - docker
script:
  - npm install -g serverless
  - npm install serverless-python-requirements
  - pip install pipenv
  - '[[ $TRAVIS_BRANCH == "master" ]] && sls deploy -v --force --conceal'
branches:
  only:
    - master
